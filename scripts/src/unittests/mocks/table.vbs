
Class Table
    Dim GlassHeight
    Dim PlayfieldMaterial
    Dim BackdropColor
    Dim SlopeMax
    Dim SlopeMin    
    Dim Inclination
    Dim FieldOfView
    Dim Layback
    Dim Rotation
    Dim Scalex
    Dim Scaley
    Dim Scalez
    Dim Xlatex
    Dim Xlatey
    Dim Xlatez                
    Dim Gravity
    Dim Friction
    Dim Elasticity
    Dim ElasticityFalloff
    Dim Scatter
    Dim DefaultScatter
    Dim NudgeTime
    Dim PlungerNormalize
    Dim PhysicsLoopTime         
    Dim PlungerFilter          
    Dim YieldTime
    Dim BallImage
    Dim BackdropImage_DT
    Dim BackdropImage_FS
    Dim BackdropImage_FSS
    Dim BackdropImageApplyNightDay
    Dim ColorGradeImage
    Dim Width
    Dim Height
    Dim MaxSeparation
    Dim ZPD
    Dim Offset
    Dim GlobalStereo3D
    Dim BallDecalMode
    Dim Image
    Dim FileName
    Dim Name
    Dim EnableAntialiasing
    Dim EnableAO
    Dim EnableFXAA
    Dim EnableSSR
    Dim BloomStrength
    Dim BallFrontDecal
    Dim OverridePhysics
    Dim OverridePhysicsFlippers
    Dim EnableEMReels
    Dim EnableDecals
    Dim ShowDT
    Dim ShowFSS
    Dim ReflectElementsOnPlayfield
    Dim EnvironmentImage
    Dim BackglassMode
    Dim Accelerometer
    Dim AccelNormalMount              
    Dim AccelerometerAngle
    Dim JoltAmount
    Dim TiltAmount
    Dim JoltTriggerTime
    Dim TiltTriggerTime
    Dim GlobalDifficulty               
    Dim TableHeight           
    Dim DeadZone             
    Dim LightAmbient
    Dim Light0Emission
    Dim LightHeight
    Dim LightRange          
    Dim EnvironmentEmissionScale
    Dim LightEmissionScale
    Dim AOScale
    Dim SSRScale        
    Dim TableSoundVolume
    Dim TableMusicVolume
    Dim TableAdaptiveVSync
    Dim BallReflection
    Dim PlayfieldReflectionStrength
    Dim BallTrail
    Dim TrailStrength
    Dim BallPlayfieldReflectionScale
    Dim DefaultBulbIntensityScale
    Dim DetailLevel
    Dim NightDay
    Dim GlobalAlphaAcc
    Dim GlobalDayNight
    Dim Version
    Dim VersionMajor
    Dim VersionMinor
    Dim VersionRevision
    Dim VPBuildVersion
End Class